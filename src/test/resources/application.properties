# H2 does not allow unquoted reserved keywords like USER and SESSION as table names.
# Enabling global identifier quoting makes Hibernate wrap every identifier in
# double-quotes (e.g. "user"), which H2 accepts.
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
